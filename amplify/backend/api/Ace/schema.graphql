type PointArray {
  date: String
  point: String
}

type ObjectArray {
  name: String
  buy_place: Int
  recycle_place: [Int]
  recycle_emelent: [String]
}

type UserData @model @auth(rules: [{allow: public, operations: [update]}]) {
  id: ID!
  pw: String
  list_object: [ObjectArray]
  point: [PointArray]
}
  