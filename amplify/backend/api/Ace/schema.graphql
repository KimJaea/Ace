type ObjectArray {
  name: String
  buy_place: Int
  recycle_place: [Int]
  recycle_element: [String]
}

type PointArray {
  date_time: String
  point: String
}

type UserData @model @auth(rules: [{allow: public}]) {
  id: ID!
  pw: String
  list_object: [ObjectArray]
  point: [PointArray]
}
 